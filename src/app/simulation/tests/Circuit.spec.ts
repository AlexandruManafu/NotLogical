import { TestBed } from '@angular/core/testing';
import { Circuit } from '../objects/Circuit';
import { Simulator } from '../objects/Simulator';



describe('Circuit', () => {
  
  let circuit : Circuit = new Circuit()

  it('A unary gate should have between 0 and 1 wires as input', () => {
    
  });

  it('A binary gate should have between 0 and 2 wires as input', () => {
    
  });

  it('Removing a gate will remove its neighbour wires', () => {
    
  });


  
});